<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="text.bwei.com.quarterhour">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".hello.HelloActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".ProductionActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AttentionActivity" />
        <activity android:name=".singup.LogintwoActivity" />
        <activity android:name=".singup.RegisterActivity" />
        <activity android:name=".singup.PwdActivity" />
        <activity android:name=".ExitActivity" />
        <activity android:name=".follow.FollowActivity" />
        <activity android:name=".CreationActivity" />
        <activity android:name=".fabiao.CrosstalkActivity" />
        <activity android:name=".SuccessActivity" />
        <activity android:name=".InvideoActivity" />
        <activity android:name=".sousuo.SeekActivity" />

    </application>

</manifest>